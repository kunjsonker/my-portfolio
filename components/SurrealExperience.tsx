// components/SurrealExperience.tsx
import SurrealExperienceClient from "./SurrealExperienceClient";

const SurrealExperience = () => {
  // This file is a Server Component by default (no "use client")
  return <SurrealExperienceClient />;
};

export default SurrealExperience;
